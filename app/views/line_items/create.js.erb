$('#cart').html("<%=j render(@cart) %>");
if ( $('#notice').length ){
    $('#notice').css("display","none");
}

if ($('#cart tr').length == 2) { $('#cart').show(); }

$('.product_<%=@product.id%>').html("Added to cart <%= pluralize @product.popularity.to_i, 'times'%>")